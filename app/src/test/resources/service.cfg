# Min value
min=10

# Max value.
max=1_000_000_000

# Number of values.
count=256_000_000

# Sort order
order=ASC

# Port to bind the service to.
port=8080

# Use all network interfaces (false) or try to detect LAN network interface and only register there
lanOnly=false
